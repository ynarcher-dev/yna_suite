# pnpm workspace — apps/*(배포 단위)와 packages/*(공통 코드)를 워크스페이스로 묶는다.
# (yna_suite_foldering.md §2, yna_suite_tech_stack.md §3)
packages:
  - "apps/*"
  - "packages/*"

# 네이티브 빌드 스크립트 실행을 허용할 의존성.
# esbuild(vitest 번들), sharp(Next 이미지 최적화)만 명시적으로 허용한다.
allowBuilds:
  esbuild: true
  sharp: true
